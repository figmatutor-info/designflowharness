# Audit Report

**검사일:** 2026-09-19
**Round:** 1 (공식 audit 기준 — STAGE=fix Round 1·2 는 이 audit 이전 사전 보정 라운드였음)
**대상:** 5개 화면 (01-home, 02-skill-library, 03-mission-detail, 04-my-assets, 05-huddling-pick)

---

## 최종 판정: PASS ✅

---

## A단계 · 사전 검사 (입력이 쓸 만한가)

| 스크립트           | 결과 | 비고                                                                                             |
| ------------------ | ---- | ------------------------------------------------------------------------------------------------ |
| check-snapshot.mjs | ✅   | 26/26 통과 · schema_version 4 · 03 Screens 5프레임/355노드, 잘림 없음, isPrimary 화면당 1개 포함 |
| check-assets.mjs   | ✅   | 8/8 통과 · §I 표 10슬롯 전부 `design/assets/characters/` 파일 존재 확인                          |

두 스크립트 모두 통과 — audit 입력을 신뢰할 수 있음.

## B단계 · 구조 검증 (스크립트 · `figma-audit.mjs`)

| 항목              | 결과 | 위반                                                 |
| ----------------- | ---- | ---------------------------------------------------- |
| 팔레트 일관성     | ✅   | 0건                                                  |
| 타이포 재사용     | ✅   | 0건                                                  |
| spacing 그리드    | ✅   | 0건                                                  |
| 탭 영역           | ✅   | 0건                                                  |
| 세이프 에어리어   | ✅   | 0건 (167개 콘텐츠 노드 검사)                         |
| primary 개수      | ✅   | 0건 (화면당 정확히 1개)                              |
| 컴포넌트 재사용률 | ✅   | 100% (기준 90%, 인스턴스 63 / 손제작 프레임 0)       |
| 토큰 계층         | ✅   | 0건 (212개 바인딩 전부 semantic, primitive 직결 0건) |
| 레이아웃 거동     | ✅   | 0건 (고정 높이·넘침 없음)                            |

**요약:** 9/9 PASS, 총 위반 0건.
**상세:** `design/04-screens/audit-structural.json`

## C단계 · 시각적 검증 (LLM)

### 01-home

- 전체 균형: OK — "이번 달 미션" 카드 → 섹션(자료/스킬/자산/알림) 순으로 위계가 명확.
- 이미지: OK — 4개 썸네일(펭귄 캐릭터 이미지) 전부 채워짐, 회색 빈 박스 없음, §I 표 파일과 일치, 톤 일관.
- 규칙 준수: OK — 색상·타이포·아이콘 크기 통일. 하단 탭바 아이콘/라벨 정렬 균등.
- 사용성: OK — primary(미션 카드)가 유일한 카드형 컴포넌트로 시각적으로 구분됨. 잘림·어색한 빈 공간 없음.

### 02-skill-library

- 전체 균형: OK — 검색바 → 카테고리 탭 → 난이도 탭 → 카드 리스트 순 위계 명확.
- 이미지: 해당 없음 (§I 표에 슬롯 없음, 확인됨).
- 규칙 준수: OK — 3개 SkillCard 스타일 일관, 무료/유료·난이도 Badge 색상 규칙대로.
- 사용성: ⚠️ 개선 여지 — 구조상 첫 카드가 `isPrimary`(primary count 검사 대상)로 지정돼 있으나, 시각적으로 나머지 두 카드와 완전히 동일한 스타일이라 육안으로는 "이게 primary"라는 구분이 없음. 다만 design-rules 의 SkillCard 정의에 primary 강조 스타일이 없어 규칙 위반은 아님 — 화면 구조 확인용 플래그와 시각적 강조가 별개 개념으로 쓰인 것으로 판단.
- 탭 영역(size-tap-min 44 반영): 시각적으로 탭이 과도하게 두꺼워 보이지 않음, 어색함 없음.

### 03-mission-detail

- 전체 균형: OK — AppBar(뒤로가기+제목) → 본문 → 관련 스킬 → 제출 상태 → 피드백 → BottomCTA 순서 자연스러움.
- 이미지: 해당 없음 (§I 표에 슬롯 없음). ItemRow 썸네일은 의도대로 중립색(color-surface-2) 채움 — 빈 이미지 슬롯 아님, 정상.
- 규칙 준수: OK — Badge(피드백 대기, warning) 색상 정확, BottomCTA 보라색 풀폭으로 규칙대로.
- 사용성: OK — primary 액션(제출하기)이 화면 하단에 고정되어 매우 눈에 띔. 콘텐츠 잘림 없음.

### 04-my-assets

- 전체 균형: OK — 상태 필터 탭 → 등록 버튼 → 자산 카드 → 수익 요약 순.
- 이미지: OK — AssetCard 2개(펭귄 캐릭터 사진) 채워짐, 회색 박스 없음, 상태 Badge(판매중/멤버공개) 대비 양호.
- 규칙 준수: OK — 색상·radius·타이포 일관.
- 사용성: ⚠️ 개선 여지 — "판매 수익 요약" 섹션 아래로 탭바까지 빈 공간이 상당히 넓어 보임(콘텐츠가 3개뿐이라 발생). 결함은 아니나 콘텐츠가 늘어나기 전까지는 다소 허전한 인상. build-log 에 이미 기록된 BottomCTA 대신 인라인 Button 배치 결정과 별개의 관찰.

### 05-huddling-pick

- 전체 균형: OK — 검색바 → 카테고리 탭 → 정렬 탭 → 픽 카드 순.
- 이미지: OK — PickCard 2개 세로형(펭귄 캐릭터 사진) + ItemRow 1개 전부 채워짐, 텍스트(가격·제목)는 이미지 밖에 배치되어 대비 문제 없음, 잘림 없음.
- 규칙 준수: OK — 가격(label bold), 카테고리 Pill, 제작자 caption 스타일 규칙대로.
- 사용성: ⚠️ 개선 여지 — 04-my-assets 와 동일하게 리스트 아래 빈 공간이 넓어 보임(콘텐츠 3개 기준). 반복되는 패턴이라 다음 라운드에서 카드 개수를 늘리거나 EmptyState/추가 섹션으로 채우는 것을 고려할 만함.

---

## 진단

**원인 분류:**

- [x] PASS — 결함 없음 (진단 분류 대상 아님)
- [ ] 국소 결함
- [ ] 방향 오류
- [ ] 반복 실패

**주요 발견:**

- 구조 검증 9/9, 사전 검사 2/2 모두 통과. 시각 검증에서 발견한 것은 전부 ⚠️ 개선 여지 수준(빈 공간, primary 카드의 시각적 무강조)이며 design-rules 위반은 아니다.
- STAGE=fix Round 1·2 에서 처리한 isPrimary 명명, Tab 탭 영역(44px), spacing semantic 바인딩 수정이 이번 audit 에서 전부 유효하게 반영된 것을 스냅샷·스크린샷 양쪽에서 확인.

## 알려진 보류 사항 (결함 아님, 후속 라운드 후보)

1. **ItemRow 마스터 썸네일 레이어명** — `02 Components` 의 ItemRow 마스터 썸네일이 `Thumb` 으로 명명되어 있고 `Img/` 접두사가 아님. 03-mission-detail 은 §I 표에 이미지 슬롯이 없어 이 화면에서는 문제되지 않음(node_id 기반 주입이라 동작 자체는 정상). 다음 컴포넌트 보완 라운드에서 `Img/item-thumb` 로 개명 권장.
2. **04-my-assets 하단 구성** — BottomCTA 대신 인라인 Button + TabBar 로 사용자 확정. 규칙 위반 아님, 재확인 완료.
3. **보조 상태 프레임 미생성** — loading/empty/submitting/error 등은 시간 예산상 생성하지 않음(5개 화면 모두 default 만). screens.md 요구사항이지만 이번 게이트 4 시연 범위에서 fail 사유로 처리하지 않음 — 필요 시 별도 라운드로 진행.
4. (신규 관찰) **04/05 화면 하단 여백** — 콘텐츠 수가 적어 탭바 위 빈 공간이 넓음. 결함은 아니지만 콘텐츠 확장 또는 섹션 추가 시 개선 여지.
5. (신규 관찰) **02-skill-library primary 카드 시각적 무강조** — 구조적으로는 정확히 1개 primary 이나, SkillCard 컴포넌트 정의 자체에 강조 variant가 없어 육안 구분 불가. design-rules 위반은 아님(규칙에 강조 요구 없음).

## 다음 조치

PASS 이므로:
→ 🎉 게이트 4 통과. 위 5개 보류/관찰 사항은 결함이 아니라 다음 라운드(있다면) 참고용으로만 남김.
→ fix-list.md 는 이번 라운드에서 신규 항목을 추가하지 않음(기존 Round 1·2 이력은 그대로 보존).
